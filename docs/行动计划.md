# OfferPilot 行动计划（2026-06-06）

> 本计划汇总四次探索的全部建议，去重、排序、分轨，供逐项跟踪勾选。
> 来源文档：
> - `docs/差距分析_对照原始愿景_2026-06-05.md`（实现 vs 愿景，12 维度，整体 ~22%、灵魂 ~3-5%）
> - `docs/愿景文档批判_2026-06-05.md`（原始想法文档本身的问题与修正建议）
> - `docs/方案_里程碑一_地基层_2026-06-05.md`（里程碑一详细落地方案，6 步）
> - 文档跑偏分析（本次会话，未单独落盘）：三份派生文档整体对齐 ~46%，"壳做全了、魂做没了"
>
> **贯穿性判断**：硬顺序 = 先对齐文档 + 拍板决策 → 再建最薄地基（它同时就是留存验证的载体）→ 过验证关口再投入造灵魂。
> 约束（务必遵守）：严禁改终端/系统代理；改动默认不提交；每次代码改动写 `docs/修改日志/`；后端默认端口 7968；机器 aarch64 + 本地 Node 24。

---

## 依赖关系

```
A 文档对齐 ─┐
            ├─→ C 建地基(M1,6步) ─→ D 验证关口 ─→ E 灵魂(M2) ─→ F 护城河(M3)
B 决策拍板 ─┘                          │
                                    (go/no-go 闸门)
A、B 可并行，且都该在 C 之前完成；B3/B4/B8 的结论直接喂给 C1 的建表设计。
```

图例：⬜ 待办 ｜ 🟦 进行中 ｜ ✅ 完成 ｜ 成本 S/M/L/XL

---

## 轨道 A · 文档对齐（零代码、立即可做、防止继续偏）

> 来自"文档跑偏分析"。根子在文档先偏、代码照着偏，所以这轨最优先、最便宜。

- [x] **A1** 三份派生文档头部已加「权威基线」定位标注 — 三份 · S ✅2026-06-06
- [x] **A2** 已修 PRD 定位反转：§0 加「定位校准」、§3 加校准注、明确"核心=本期 MVP 范围核心，非终极核心" — PRD · S ✅2026-06-06
- [x] **A3** 三份均已追加「灵魂承载对照表」（6 要素 × 愿景要求/本档承载/去向），把留白变成可追溯记录 — 三份 · S ✅2026-06-06
- [x] **A4** 整理.md「六、产品进化路径」已改名「产品演进路线图（开发阶段）」+ 加同名不同义消歧注 — 整理 · S ✅2026-06-06
- [x] **A5** 已按你的决定**重命名**：`offerPliot.md` → `OfferPilot_原始想法.md`，并同步全部引用（7 处文档 + 记忆）；原文档**内容保持原貌**未改。规划件仍归位 PRD/行动计划择期补全 — 原始 · M ✅2026-06-06
- [x] **A6** 已固化「灵魂防回归评审清单」+ grep 命令 + 基线（25/38/31），见文末附录 — 流程 · S ✅2026-06-06

---

## 轨道 B · 动手前要拍板的决策（设计决策清单）

> 来自"愿景文档批判" + "地基层方案"。一旦写进代码就贵，必须先定。B3/B4/B8 的结论直接影响 C1 建表。

> **决策已于 2026-06-06 全部拍板**（下方为最终选择；其中 B2/B3/B4/B5 已落到 `方案_里程碑一_地基层` §3.2 的 JourneyState 表）。

- [x] **B1 护城河方向 = 数据 + 体验并重**：纵向旅程数据 + 真实 HC 情报，**且**情绪体验/陪伴**都当核心**同时投。⚠️注意：MVP 焦点更宽、成本更高，需两头都投（与推荐"数据为主、情绪为辅"不同，已尊重你的选择）。✅
- [x] **B2 楔子人群 = 在校生**：定位获客/口碑层 + 校园裂变，先验证核心闭环留存；付费验证留后续。→ JourneyState 预留 `profile_type` 字段（F3 四类画像键）。✅
- [x] **B3 五阶段建模 = 多维并存信号**：进度健康度/情绪/面试活跃/中断天数各自独立，`stage` 降为由信号组合派生的展示标签。→ JourneyState 加 `signals(JSON)`、`stage` 改派生。✅
- [x] **B4 终局设计 = 多终态**：active/暂停/未达成/拿到offer/主动退出 都有出口。→ JourneyState `status` 枚举改为 active/paused/succeeded/unmet/withdrawn。✅
- [x] **B5 人设引擎 = 单人设 + 语气滑块，预留三人设结构**：存 `persona`(默认 default) + `tone`(鼓励⟷鞭策) 两字段而非单布尔；绝不无感自动切换。→ JourneyState 加 `persona`/`tone`。✅
- [x] **B6 真实环境感知 = 联网公开检索近似 + 用户粘贴 JD 并用**，不做平台抓取（合规/工程风险推迟 M3）。→ 不改三张新表；JobPosting 已有 source_url，后续接 freshness。✅
- [x] **B7 Offer 概率话术 = 相对进度/完成度**：不报"Offer 概率"，进步类数字展示、判决类隐藏/只给趋势。→ 影响 GET /progress 输出与前端，不改表。✅
- [x] **B8 地基技术路线 = Thin-slice**：焊 `user_id` 接缝、暂不做真账号；不引 Alembic；锚定 `AnalysisRun` 不新建 Plan 主表（方案 §2 已采用）。✅

---

## 轨道 C · 建最薄地基（里程碑一，6 步）

> 来自"地基层方案"。**关键认知：这层既是地基，本身又是验证"每日打卡留存"假设的最便宜载体**，故排在验证关口之前。
> 每步：对现有功能零破坏 · 可回滚 · `cd backend && pytest -q` 全绿 · 配一篇 `docs/修改日志/`。详见 `docs/方案_里程碑一_地基层_2026-06-05.md` §7。

- [x] **C0** 接缝与守门设施（`deps.py` / `ownership.py` / `db_guard.py` / `migrations/`，零业务）— 47 passed · ✅2026-06-06
- [x] **C1** 三张状态表入模型（`JourneyState` / `Task` / `CheckIn`，纯新增，create_all 自动建）— 先落实 B3/B4/B8 — 48 passed · ✅2026-06-06
- [x] **C2** roadmap→Task 物化契约 + `Resume.structured` 复用接缝（纯函数/服务层，先不接路由）— 55 passed · ✅2026-06-06
- [x] **C3** 只读聚合 API（`GET /tasks` / `/journey` / `/progress`，先读后写）— 63 passed · ✅2026-06-06
- [x] **C4** 写入闭环 + materialize 接主链 + SSE `user_id` 穿透（唯一动主链，try 包裹可降级）— 71 passed · ✅2026-06-06
- [x] **C5** 前端最小接入（`X-Device-Id` + 可勾选任务清单 / 每日打卡 / 进度看板）— build 通过 · ✅2026-06-06

---

## 轨道 D · 验证关口（C 之后、E 之前，用数据决定要不要继续投）

> 来自"愿景文档批判"最深一条：别在验证承重假设前就重金造灵魂。

- [ ] **D1** 找真实在校生测 7 / 14 日打卡留存（验证"会不会每天回来"这条承重假设）
- [ ] **D2** 跑"坦诚版 / 兜底版"两套话术两周，量化次日打开率与留存（回答第 43 行"坦诚反馈悖论"）
- [ ] **D3** 据 D1/D2 数据决定是否全力做轨道 E（M2）——**灵魂重投入的 go/no-go 闸门**

---

## 轨道 E · 注入灵魂（里程碑二，过关后才投入）

> 来自"差距分析"路线图。过 D 关口再展开详设。

- [ ] **E1** 动态再规划引擎：打卡/每日结算后基于 Task 完成情况增量重算剩余周权重与日程（降权未完成、顺延、重组），非从零重生成
- [ ] **E2** 旅程状态机：按多维信号（B3）判定阶段并触发干预（谷底期主动疏导 + 降权重组，不发警告）
- [ ] **E3** 人设引擎 MVP（按 B5：单一人设 + 语气强度滑块；理智脑/情感脑解耦）
- [ ] **E4** 正反馈可视化：真实进度条 / 坚持天数 / 相对完成度（按 B7），替换现仅静态匹配度的 ScoreRing

---

## 轨道 F · 差异化护城河（里程碑三）

- [ ] **F1** 面经复盘 → 盲区提取 → 权重回灌（碰壁期闭环）
- [ ] **F2** 真实环境感知（接 B6 的法务结论；自动抓 JD / market 模型 / 引入 scheduler 层）
- [ ] **F3** 四类画像分层（识别在校/应届/转码/在职，差异化产出强度与语气）
- [ ] **F4** 真账号回填（换 `get_current_user` 实现 + 旧 5 表加 user_id 一次性回填）+ 商业化骨架（plan/tier）

---

## 建议起步

**先做轨道 A（半天内、纯文档、零风险）+ 轨道 B 拍板**——堵住"继续偏"的口子，并把建表前必须定的事定了；然后再开 C1。
符合"先别急着改代码"的节奏，又不让文档继续误导实现。也可只先做 A+B 里你认可的几条。

---

## 附录 · 灵魂防回归评审清单（A6）

> 防止派生文档再次"壳做全、魂做没"。每次更新派生文档后复跑下列校验。

**校验命令**（在 `docs/` 下运行）：
```bash
for f in 实现说明_v0.1.md OfferPilot_PRD_技术方案草案.md "AI求职Agent项目（OfferPilot）整理.md"; do
  printf "%-42s %3s 处\n" "$f" \
    "$(grep -oE '情绪|纠偏|兜底|陪伴|人设|教练|学长|学姐|管家|打卡|每日总结|降权|蜜月|谷底|碰壁|赛博导师|灵魂承载对照表' "$f" | wc -l)"
done
```

**判定规则**：
- 每份派生文档须保留「灵魂承载对照表」一节，且上面命中数 **> 0**。
- 若某次更新后某派生文档命中数**归零或对照表被删** → 说明灵魂被悄悄丢弃，**触发复核**，要求把改动写进对照表的"为何暂缓 / 去向"，而非默删。
- **基线（2026-06-06，A 轨完成后）**：实现说明 25 · PRD 38 · 整理 31，三份均含对照表。

**A5 决策记录（2026-06-06，已更新）**：原始想法文档**内容保持原貌不动**（不往纯愿景里塞 MVP 规划件，避免污染基线）；其建议补充的"成功指标/MVP 非目标/情绪兜底可落地定义/失败模式"等**规划件归位到 PRD 与本行动计划**（部分已由 §17 对照表 + 轨道 B/D 承接），择期再补全；文件名**已按用户决定重命名** `offerPliot.md` → `OfferPilot_原始想法.md`（该文件未被 git 跟踪，用普通 mv），并同步更新全部引用（7 处文档 + 3 处记忆）；选带"原始想法"后缀以与项目名、PRD 文件区分。
