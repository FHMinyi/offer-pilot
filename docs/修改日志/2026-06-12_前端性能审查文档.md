# 修改日志 · 2026-06-12（前端性能审查文档）

## 修改内容

- 新增 `docs/前端性能审查_2026-06-12.md` —— ChatView 拆分重构后（57ba256 基线）的前端
  性能专项审查报告：
  - 2 条高危（流式渲染 O(n²) 主瓶颈 + DOMPurify 钩子无界累积放大器，两者相乘）、
    5 条中等（每轮全量 POST / 卸载不中止 SSE / 路由往返重拉 / 载入全量渲染 / 列表重拉）、
    5 条 low、1 条被对抗核实驳回的猜想留档；
  - 全部关键结论带无头浏览器实测数字（平方放大 115×、钩子放大 18.3×、上送有效率 0.04%、
    首帧 348ms、首屏 92KB gzip 等）；
  - 检查过且健康的 10 个方面单列（防止未来重复怀疑）；
  - 按性价比排序的修复清单（前两项合计改动极小即可消除全部流式高危）。

仅新增文档，未改任何代码。

## 原因

- ChatView 拆分重构刚完成，用户关心当前前端是否有潜在性能问题；趁实测脚手架
  （隔离栈 + Playwright 插桩）现成，做一次带数字的体检并留档。
- 流式对话是产品核心体验，O(n²) 瓶颈随输出长度恶化，宜在接长思考模型/长报告前修掉。

## 抉择

- **静态分析 + 隔离栈实测 + 对抗核实三阶段**：性能问题最容易「静态推理看似成立、实际被
  Vue/浏览器机制化解」——本次就有 1 条猜想（流式期 layout 随 DOM 总量变慢）被实测驳回，
  双重天然化解（Vue 精确依赖 + 增量布局）如实留档。
- **实测在隔离栈进行**：临时后端/临时 sqlite/临时前端端口，造数 50 轮 420KB 大会话不污染
  开发数据库；v-if 对照实验临时改文件、测后 md5 校验恢复。
- **dev 构建数字如实标注**：生产构建绝对值略好，但平方/线性增长的形态与倍数关系不变，
  结论不受影响。
- 修复本身不在本次范围（用户先要报告），修复清单按性价比排序留给后续决策。

Agent-Task: 评估前端潜在性能问题 + 整理成审查文档
Agent-Model: claude-fable-5
Agent-Decision: 三阶段（静态/实测/对抗核实）而非纯静态推理；隔离栈实测防污染；被驳回猜想留档防重提
Agent-Limitation: 实测基于 dev 构建与 headless Chromium 单环境；实测脚本留存 /tmp 未入库（报告附录记录了复建方法）
